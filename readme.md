## Installation

```
$ npm install
$ bower install
```


```
express mean-project
cd mean-project/
npm install
npm install --save-dev gulp
bower init
bower install angular --save
bower install angular-route --save
npm install mongoose --save
npm install gulp-sass --save-dev
```